{
	"name": "homebuild-oidc-provider",
	"version": "0.0.1",
	"description": "An implementation of Express, PassportJs and OIDC-Provider to provide a reusable and extendable OIDC Provider",
	"main": "server.js",
	"scripts": {
		"start": "set DEPLOYMENT_ENV=local && nodemon server",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"OIDC",
		"Express",
		"PassportJS",
		"OIDC-Provider",
		"OAuth2.0"
	],
	"author": "Matthew Folker",
	"license": "MIT",
	"dependencies": {
		"bootstrap": "^4.4.1",
		"express": "^4.17.1",
		"express-http-context": "^1.2.3",
		"lodash": "^4.17.15",
		"log4js": "^6.1.1",
		"oidc-provider": "^6.20.2",
		"querystring": "^0.2.0",
		"uuid": "^3.4.0"
	},
	"devDependencies": {
		"aws-sdk": "^2.631.0",
		"nodemon": "^2.0.2"
	}
}
